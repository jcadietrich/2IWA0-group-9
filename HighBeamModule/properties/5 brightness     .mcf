[ ExtLampSwitch(Auto)* .
!ExtLampSwitch(On)*.
KeyState(Ignition).
!(KeyState(Inserted)||ExtLampSwitch(On))*.
Brightness(Low)]
<!(KeyState(Inserted)||ExtLampSwitch(On)||Brightness(High))*
. LowBeam(On)>true

&&
[true*. ExtLampSwitch(Auto) .
!(ExtLampSwitch(On))*
. Brightness(Low). 
!(Brightness(High)||ExtLampSwitch(On))* .
KeyState(Ignition)]
<(KeyState(Inserted)||ExtLampSwitch(On)||Brightness(High))*
. LowBeam(On)>true

&&
[true*. Brightness(Low). 
!Brightness(High)* . 
ExtLampSwitch(Auto) .
!(ExtLampSwitch(On) ||Brightness(High))*.
KeyState(Ignition)]
<(KeyState(Inserted)||ExtLampSwitch(On) ||Brightness(High))*
. LowBeam(On)>true

&&
[true*. Brightness(Low). 
!Brightness(High)* .
KeyState(Ignition) .
!(KeyState(Inserted)||Brightness(High))* . 
ExtLampSwitch(Auto)]
<(KeyState(Inserted)||ExtLampSwitch(On) ||Brightness(High))*
. LowBeam(On)>true

&&
[true*. KeyState(Ignition) .
!KeyState(Inserted)*.
ExtLampSwitch(Auto) .
!(ExtLampSwitch(On) ||KeyState(Inserted))*.
Brightness(Low)]
<(KeyState(Inserted)||ExtLampSwitch(On) ||Brightness(High))*
. LowBeam(On)>true

&&
[true*. KeyState(Ignition) .
!KeyState(Inserted)*.
Brightness(Low).
!(Brightness(High)||KeyState(Inserted))*.
ExtLampSwitch(Auto)]
<(KeyState(Inserted)||ExtLampSwitch(On) ||Brightness(High))*
. LowBeam(On)>true

